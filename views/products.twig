<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
</head>
<body>
    <h2 style="text-align: center; color: red; margin-top: 20px;"> Product list </h2>
    <table border="1" style="width: max-content; display: block; margin: auto;">
        <thead>
            <th> Title </th>
            <th> Price </th>
            <th> Show </th>
            <th> Delete </th>
        </thead>
        <tbody>
            {% for item in products %}
                <tr>
                    <td> {{ item.title }} </td>
                    <td> {{ item.price }} </td>
                    <td> <a href="/{{ item._id }}"> show </a> </td>
                    <td> <a href="/delete/{{ item._id }}"> delete </a> </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

</body>
</html>